package Board;

public class Tracker {
    // instantiated at beginning of simulation run and stay active until end
    // subscribe for published events
    // maintain a data struct in memory for current status (frame state)
    // responsible for printing a frame state summary (i.e. the console printing)
}
